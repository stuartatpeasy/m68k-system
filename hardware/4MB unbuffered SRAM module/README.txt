2014-05-25

I think it's very unlikely that this module would work.  The bus loading is excessive, and no buffers are used in the design.  Use the buffered version of th 4MB module instead.

This PCB was never sent to production, so the design has not been tested.